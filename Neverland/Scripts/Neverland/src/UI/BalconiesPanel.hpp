#pragma once

#include "ArchUICommon.hpp"

// Раздел «Балконы» (в разработке): плита с ограждением на кронштейнах.
class BalconiesPanel final : public ArchPanel {
public:
    explicit BalconiesPanel(const ArchUIContext &context);
};
