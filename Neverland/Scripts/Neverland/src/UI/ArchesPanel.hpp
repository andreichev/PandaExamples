#pragma once

#include "ArchUICommon.hpp"

// Раздел «Арки» (в разработке): арочные проёмы линии стен.
class ArchesPanel final : public ArchPanel {
public:
    explicit ArchesPanel(const ArchUIContext &context);
};
