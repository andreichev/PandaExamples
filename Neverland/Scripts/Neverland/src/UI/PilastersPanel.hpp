#pragma once

#include "ArchUICommon.hpp"

// Раздел «Пилястры» (в разработке): плоские колонны на плоскости стены.
class PilastersPanel final : public ArchPanel {
public:
    explicit PilastersPanel(const ArchUIContext &context);
};
