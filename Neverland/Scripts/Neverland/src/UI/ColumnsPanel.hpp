#pragma once

#include "ArchUICommon.hpp"

// Раздел «Колонны» (в разработке): классические ордера.
class ColumnsPanel final : public ArchPanel {
public:
    explicit ColumnsPanel(const ArchUIContext &context);
};
